let getThemesHTML,createDir,getIndexHTML;_6d9‍.w("./html/html-themes",[["getThemesHTML",["getThemesHTML"],function(v){getThemesHTML=v}]]);_6d9‍.w("./utils/dir",[["createDir",["createDir"],function(v){createDir=v}]]);_6d9‍.w("./html/html-page",[["getIndexHTML",["getIndexHTML"],function(v){getIndexHTML=v}]]);const fs = require('fs');
const nodeEval = require('node-eval');





const PATH_PAGES = 'build';

createDir(PATH_PAGES);

nodeEval(fs.readFileSync('./@modules/@links.js', 'utf8'));

ARR_LINKS.forEach(module => {
    if (module) {
        const { html, menu } = getThemesHTML(module.page);

        const page = getIndexHTML({
            up: '../',
            title: module.title,
            script: module.page,
            menu,
            content: html,
        });
        
        fs.writeFileSync(`${PATH_PAGES}/${module.page}.html`, page);
    }
})
