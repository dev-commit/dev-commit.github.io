_863‍.x([["createDir",()=>createDir]]);const fs = require('fs');
const rimraf = require('rimraf');

       const createDir = (path) => {
    rimraf.sync(path);

    if (!fs.existsSync(path)) {
        fs.mkdirSync(path);
    }
}
