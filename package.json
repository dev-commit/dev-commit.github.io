{
  "name": "workbook",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "typecheck": "tsc --noEmit",
    "preview": "vitepress preview docs",
    "build-gh-pages": "gh-pages -d docs/.vitepress/dist",
    "commit": "git add . && git commit -m \"Bash Script Commit\" && git push origin head",
    "deploy": "npm run build && npm run build-gh-pages && npm run commit"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "gh-pages": "^6.1.1",
    "typescript": "^5.7.2",
    "vitepress": "^1.6.0",
    "vue": "^3.4.0",
    "vue-router": "^5.0.3"
  }
}
